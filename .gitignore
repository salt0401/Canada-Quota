__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Transient atomic-save file (data/canada_trq_tracker.xlsx.tmp); must never be
# committed by the CI's `git add data/` if a crash leaves one behind.
*.tmp

# Distribution bundle — derived from download_latest.bat + guide.html, regenerated as needed
download_latest.zip
